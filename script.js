function readText() {
        const utterance = new SpeechSynthesisUtterance("This is the text that will be read aloud.");
        speechSynthesis.speak(utterance);
    }
    function readText() {
        const utterance = new SpeechSynthesisUtterance("This is the text that will be read aloud.");
        speechSynthesis.speak(utterance);
    }
        